{
  "name": "@cogent/test-component",
  "version": "0.0.0",
  "description": "A simple test component",
  "main": "lib/TestComponent.js",
  "main:src": "src/TestComponent.js",
  "license": "COGENT",
  "dependencies": {
    "@babel/cli": "^7.5.5"
  },
  "scripts": {
    "transpile": "babel src -d lib --ignore '**/*.spec.js,**/*.stories.js'",
    "jest": "jest --coverage --verbose --color"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-react"
    ],
    "env": {
      "test": {
        "plugins": [
          "transform-es2015-modules-commonjs"
        ]
      }
    }
  }


}
